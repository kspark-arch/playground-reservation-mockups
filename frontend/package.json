{
  "name": "yeonsung-ground-reservation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "연성대학교 운동장 사용 예약 시스템",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "node ./vendor/serve/bin.js -s dist -l tcp://0.0.0.0:4173",
    "start": "node ./vendor/serve/bin.js -s dist -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "file:./vendor/serve"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  },
  "engines": {
    "node": ">=18"
  }
}
